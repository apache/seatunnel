# DataX 批处理环境配置模板
# 用于批量数据处理场景
# 模板类型: Batch Environment
# 版本: 1.0

env {
  # 并行度配置 - 从DataX channel数量映射
  parallelism = {{ datax.job.setting.speed.channel | default(1) }}

  # 任务模式：批处理
  job.mode = "BATCH"
}