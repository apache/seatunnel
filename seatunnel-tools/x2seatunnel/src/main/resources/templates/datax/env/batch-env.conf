# DataX Batch Processing Environment Configuration Template
# For batch data processing scenarios
# Template Type: Batch Environment
# Version: 1.0

env {
  # Parallelism configuration - mapped from DataX channel count
  parallelism = {{ datax.job.setting.speed.channel | default(1) }}

  # Job mode: batch processing
  job.mode = "BATCH"
}