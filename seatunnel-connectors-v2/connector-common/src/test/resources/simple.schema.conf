schema {
  fields {
    map = "map<string, string>"
    array = "array<tinyint>"
    string = string
    boolean = boolean
    tinyint = tinyint
    smallint = smallint
    int = int
    bigint = bigint
    float = float
    double = double
    decimal = "decimal(30, 8)"
    null = null
    bytes = bytes
    date = date
    time = time
    timestamp = timestamp
  }
  format = text
  delimiter = ","
}