<?xml version="1.0" encoding="UTF-8"?>
<!--

    Licensed to the Apache Software Foundation (ASF) under one or more
    contributor license agreements.  See the NOTICE file distributed with
    this work for additional information regarding copyright ownership.
    The ASF licenses this file to You under the Apache License, Version 2.0
    (the "License"); you may not use this file except in compliance with
    the License.  You may obtain a copy of the License at

       http://www.apache.org/licenses/LICENSE-2.0

    Unless required by applicable law or agreed to in writing, software
    distributed under the License is distributed on an "AS IS" BASIS,
    WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
    See the License for the specific language governing permissions and
    limitations under the License.

-->
<project xmlns="http://maven.apache.org/POM/4.0.0" xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
         xsi:schemaLocation="http://maven.apache.org/POM/4.0.0 http://maven.apache.org/xsd/maven-4.0.0.xsd">
    <modelVersion>4.0.0</modelVersion>
    <parent>
        <groupId>org.apache.seatunnel</groupId>
        <artifactId>connector-cdc</artifactId>
        <version>${revision}</version>
    </parent>

    <artifactId>connector-cdc-opengauss</artifactId>
    <name>SeaTunnel : Connectors V2 : CDC : Opengauss</name>

    <properties>
        <opengauss.version>5.1.0</opengauss.version>
        <!--
            This connector recompiles Debezium's PostgreSQL connection internals (see
            src/main/java/io/debezium/connector/postgresql), so its Debezium version must stay in
            step with connector-cdc-postgres. It is declared here rather than inherited so that the
            coupling is visible at the point where it has to be maintained.
        -->
        <debezium.version>1.9.8.Final</debezium.version>
    </properties>

    <dependencies>
        <dependency>
            <groupId>org.apache.seatunnel</groupId>
            <artifactId>connector-cdc-base</artifactId>
            <version>${project.version}</version>
            <scope>provided</scope>
        </dependency>

        <!--
            Debezium runtime owned by this connector. connector-cdc-base declares these as provided,
            so the copies packaged into connector-cdc-opengauss are the ones this connector runs on,
            at the ${debezium.version} declared above.
        -->
        <dependency>
            <groupId>io.debezium</groupId>
            <artifactId>debezium-api</artifactId>
        </dependency>
        <dependency>
            <groupId>io.debezium</groupId>
            <artifactId>debezium-embedded</artifactId>
        </dependency>
        <dependency>
            <groupId>com.github.luben</groupId>
            <artifactId>zstd-jni</artifactId>
        </dependency>

        <dependency>
            <groupId>org.apache.seatunnel</groupId>
            <artifactId>connector-jdbc</artifactId>
            <version>${project.version}</version>
        </dependency>

        <dependency>
            <groupId>org.opengauss</groupId>
            <artifactId>opengauss-jdbc</artifactId>
            <version>${opengauss.version}</version>
        </dependency>

        <dependency>
            <groupId>org.apache.seatunnel</groupId>
            <artifactId>connector-cdc-postgres</artifactId>
            <version>${project.version}</version>
        </dependency>

    </dependencies>

    <build>
        <plugins>
            <!-- Shade the driver of Opengauss to prevent the conflict of the Postgres's Driver -->
            <plugin>
                <groupId>org.apache.maven.plugins</groupId>
                <artifactId>maven-shade-plugin</artifactId>
                <executions>
                    <execution>
                        <goals>
                            <goal>shade</goal>
                        </goals>
                        <phase>package</phase>
                        <configuration>
                            <createSourcesJar>false</createSourcesJar>
                            <shadeSourcesContent>true</shadeSourcesContent>
                            <shadedArtifactAttached>false</shadedArtifactAttached>
                            <createDependencyReducedPom>false</createDependencyReducedPom>
                            <filters>
                                <filter>
                                    <artifact>*:*</artifact>
                                    <excludes>
                                        <exclude>META-INF/*.SF</exclude>
                                        <exclude>META-INF/*.DSA</exclude>
                                        <exclude>META-INF/*.RSA</exclude>
                                    </excludes>
                                </filter>
                                <!--
                                    Restated here because this execution declares its own filter
                                    list, which takes precedence over the one inherited from the
                                    connector-cdc parent. See that pom for why the stock copies of
                                    connector-cdc-base's patched Debezium classes must be dropped.
                                -->
                                <filter>
                                    <artifact>io.debezium:debezium-core</artifact>
                                    <excludes>
                                        <exclude>io/debezium/connector/base/ChangeEventQueue.class</exclude>
                                        <exclude>io/debezium/connector/base/ChangeEventQueue$*.class</exclude>
                                        <exclude>io/debezium/relational/TableId.class</exclude>
                                        <exclude>io/debezium/relational/TableId$*.class</exclude>
                                        <exclude>io/debezium/relational/HistorizedRelationalDatabaseConnectorConfig.class</exclude>
                                        <exclude>io/debezium/relational/HistorizedRelationalDatabaseConnectorConfig$*.class</exclude>
                                        <exclude>io/debezium/heartbeat/HeartbeatFactory.class</exclude>
                                        <exclude>io/debezium/heartbeat/HeartbeatFactory$*.class</exclude>
                                    </excludes>
                                </filter>
                            </filters>
                            <relocations>
                                <relocation>
                                    <pattern>org.postgresql</pattern>
                                    <shadedPattern>${seatunnel.shade.package}.org.postgresql</shadedPattern>
                                </relocation>
                            </relocations>
                        </configuration>
                    </execution>
                </executions>
            </plugin>
        </plugins>
    </build>

</project>
