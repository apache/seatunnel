<?xml version="1.0" encoding="UTF-8"?>
<!--

    Licensed to the Apache Software Foundation (ASF) under one or more
    contributor license agreements.  See the NOTICE file distributed with
    this work for additional information regarding copyright ownership.
    The ASF licenses this file to You under the Apache License, Version 2.0
    (the "License"); you may not use this file except in compliance with
    the License.  You may obtain a copy of the License at

       http://www.apache.org/licenses/LICENSE-2.0

    Unless required by applicable law or agreed to in writing, software
    distributed under the License is distributed on an "AS IS" BASIS,
    WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
    See the License for the specific language governing permissions and
    limitations under the License.

-->
<project xmlns="http://maven.apache.org/POM/4.0.0" xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
         xsi:schemaLocation="http://maven.apache.org/POM/4.0.0 http://maven.apache.org/xsd/maven-4.0.0.xsd">
    <modelVersion>4.0.0</modelVersion>
    <parent>
        <groupId>org.apache.seatunnel</groupId>
        <artifactId>seatunnel-connectors-v2</artifactId>
        <version>${revision}</version>
    </parent>
    <artifactId>connector-cdc</artifactId>
    <packaging>pom</packaging>
    <name>SeaTunnel : Connectors V2 : CDC :</name>

    <modules>
        <module>connector-cdc-base</module>
        <module>connector-cdc-db2</module>
        <module>connector-cdc-mysql</module>
        <module>connector-cdc-sqlserver</module>
        <module>connector-cdc-mongodb</module>
        <module>connector-cdc-postgres</module>
        <module>connector-cdc-oracle</module>
        <module>connector-cdc-opengauss</module>
        <module>connector-cdc-tidb</module>
        <module>connector-cdc-vitess</module>
    </modules>

    <properties>
        <!--
            Baseline Debezium version. Every CDC connector module is expected to re-declare this
            property so that its Debezium version is owned by the connector rather than inherited
            implicitly from this parent. The value here is only the fallback for modules that have
            not opted into an explicit declaration yet.
        -->
        <debezium.version>1.9.8.Final</debezium.version>
        <antlr.version>4.8</antlr.version>
    </properties>

    <dependencyManagement>
        <dependencies>
            <dependency>
                <groupId>org.antlr</groupId>
                <artifactId>antlr4</artifactId>
                <version>${antlr.version}</version>
            </dependency>
            <!--
                Debezium dependencies are managed here but deliberately not declared here. Inherited
                dependency management is interpolated against the effective properties of the module
                being built, so each connector resolves these artifacts at its own
                ${debezium.version} while sharing a single copy of the exclusion rules.
            -->
            <dependency>
                <groupId>io.debezium</groupId>
                <artifactId>debezium-api</artifactId>
                <version>${debezium.version}</version>
            </dependency>
            <dependency>
                <groupId>io.debezium</groupId>
                <artifactId>debezium-embedded</artifactId>
                <version>${debezium.version}</version>
                <exclusions>
                    <exclusion>
                        <groupId>org.apache.kafka</groupId>
                        <artifactId>kafka-log4j-appender</artifactId>
                    </exclusion>
                    <exclusion>
                        <groupId>org.glassfish.jersey.core</groupId>
                        <artifactId>*</artifactId>
                    </exclusion>
                    <exclusion>
                        <!--The lower version is no longer compatible with Apple M1-->
                        <groupId>com.github.luben</groupId>
                        <artifactId>zstd-jni</artifactId>
                    </exclusion>
                </exclusions>
            </dependency>
            <dependency>
                <groupId>com.github.luben</groupId>
                <artifactId>zstd-jni</artifactId>
                <version>1.5.5-5</version>
            </dependency>
        </dependencies>
    </dependencyManagement>

    <build>
        <pluginManagement>
            <plugins>
                <plugin>
                    <groupId>org.antlr</groupId>
                    <artifactId>antlr4-maven-plugin</artifactId>
                    <version>${antlr.version}</version>
                    <configuration>
                        <sourceDirectory>src/main/antlr4</sourceDirectory>
                        <outputDirectory>src/main/java</outputDirectory>
                        <listener>true</listener>
                        <visitor>true</visitor>
                        <treatWarningsAsErrors>true</treatWarningsAsErrors>
                    </configuration>
                    <executions>
                        <execution>
                            <goals>
                                <goal>antlr4</goal>
                            </goals>
                        </execution>
                    </executions>
                </plugin>
            </plugins>
        </pluginManagement>

        <plugins>
            <plugin>
                <groupId>org.apache.maven.plugins</groupId>
                <artifactId>maven-shade-plugin</artifactId>
                <configuration>
                    <!--
                        connector-cdc-base recompiles a small set of Debezium classes under
                        src/main/java/io/debezium and ships its patched copies. While Debezium was
                        packaged inside connector-cdc-base, the shade plugin resolved that overlap at
                        build time and the patched copies were the only ones that existed.

                        Now that each connector packages its own Debezium, the stock copies would
                        arrive in the connector jar while the patched ones stay in
                        connector-cdc-base, and both jars are on the same class loader. Which copy
                        wins would come down to the order File#listFiles returns them in
                        AbstractPluginDiscovery, so the patches could silently stop applying.
                        Dropping the stock copies keeps connector-cdc-base's patched classes the
                        single definition, exactly as before.

                        These class names are pinned to connector-cdc-base/src/main/java/io/debezium
                        and must be kept in step with it.
                    -->
                    <filters combine.children="append">
                        <filter>
                            <artifact>io.debezium:debezium-core</artifact>
                            <excludes>
                                <exclude>io/debezium/connector/base/ChangeEventQueue.class</exclude>
                                <exclude>io/debezium/connector/base/ChangeEventQueue$*.class</exclude>
                                <exclude>io/debezium/relational/TableId.class</exclude>
                                <exclude>io/debezium/relational/TableId$*.class</exclude>
                                <exclude>io/debezium/relational/HistorizedRelationalDatabaseConnectorConfig.class</exclude>
                                <exclude>io/debezium/relational/HistorizedRelationalDatabaseConnectorConfig$*.class</exclude>
                                <exclude>io/debezium/heartbeat/HeartbeatFactory.class</exclude>
                                <exclude>io/debezium/heartbeat/HeartbeatFactory$*.class</exclude>
                            </excludes>
                        </filter>
                        <!--
                            Restated from the root pom so the signature files stay excluded whether
                            or not the inherited filter list is merged into this one.
                        -->
                        <filter>
                            <artifact>*:*</artifact>
                            <excludes>
                                <exclude>META-INF/*.SF</exclude>
                                <exclude>META-INF/*.DSA</exclude>
                                <exclude>META-INF/*.RSA</exclude>
                            </excludes>
                        </filter>
                    </filters>
                </configuration>
            </plugin>
        </plugins>
    </build>
</project>
