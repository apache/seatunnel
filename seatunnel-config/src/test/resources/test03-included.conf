# this file is used to include in test03.conf

foo="This is in the included file"

# foo is in this file, so "a" should always be our local ${foo}
a=${foo}

# bar is only in the parent file, so "b" should always be the root ${bar}
b=${bar}
