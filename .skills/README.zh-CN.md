# .skills 目录说明

这个目录用于存放当前项目专用的本地 skill。

每个 skill 都应放在独立子目录中，并且以 `SKILL.md` 作为入口文件。
只有在确实需要复用时，才添加辅助资源，例如：

- `agents/openai.yaml`：用于界面展示的元数据
- `scripts/`：可重复执行的自动化脚本
- `references/`：任务相关的补充说明
- `assets/`：模板或输出资源

这里应优先放与仓库约定、开发流程、重复性任务直接相关的 skill。
不要在这里放无关说明、临时文件或通用项目文档。
