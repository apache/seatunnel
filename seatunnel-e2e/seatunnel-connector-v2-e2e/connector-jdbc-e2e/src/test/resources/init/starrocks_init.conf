#
# Licensed to the Apache Software Foundation (ASF) under one or more
# contributor license agreements.  See the NOTICE file distributed with
# this work for additional information regarding copyright ownership.
# The ASF licenses this file to You under the Apache License, Version 2.0
# (the "License"); you may not use this file except in compliance with
# the License.  You may obtain a copy of the License at
#
#     http://www.apache.org/licenses/LICENSE-2.0
#
# Unless required by applicable law or agreed to in writing, software
# distributed under the License is distributed on an "AS IS" BASIS,
# WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
# See the License for the specific language governing permissions and
# limitations under the License.
#

table_source_ddl = """
create table if not exists `test`.`e2e_table_source`
(
    BIGINT_COL          BIGINT,
    LARGEINT_COL        LARGEINT,
    SMALLINT_COL        SMALLINT,
    TINYINT_COL         TINYINT,
    BOOLEAN_COL         BOOLEAN,
    DECIMAL_COL         DECIMAL,
    DOUBLE_COL          DOUBLE,
    FLOAT_COL           FLOAT,
    INT_COL             INT,
    CHAR_COL            CHAR,
    VARCHAR_11_COL      VARCHAR(11),
    STRING_COL          STRING,
    DATETIME_COL        DATETIME,
    DATE_COL            DATE
)ENGINE=OLAP
DUPLICATE KEY(`BIGINT_COL`)
DISTRIBUTED BY HASH(`BIGINT_COL`) BUCKETS 1
PROPERTIES (
"replication_num" = "1",
"in_memory" = "false",
"storage_format" = "DEFAULT"
)
"""

table_sink_ddl = """
create table if not exists `test`.`e2e_table_sink`
(
    BIGINT_COL          BIGINT,
    LARGEINT_COL        LARGEINT,
    SMALLINT_COL        SMALLINT,
    TINYINT_COL         TINYINT,
    BOOLEAN_COL         BOOLEAN,
    DECIMAL_COL         DECIMAL,
    DOUBLE_COL          DOUBLE,
    FLOAT_COL           FLOAT,
    INT_COL             INT,
    CHAR_COL            CHAR,
    VARCHAR_11_COL      VARCHAR(11),
    STRING_COL          STRING,
    DATETIME_COL        DATETIME,
    DATE_COL            DATE
)ENGINE=OLAP
DUPLICATE KEY(`BIGINT_COL`)
DISTRIBUTED BY HASH(`BIGINT_COL`) BUCKETS 1
PROPERTIES (
"replication_num" = "1",
"in_memory" = "false",
"storage_format" = "DEFAULT"
)
"""
// only need for source
DML = """
INSERT INTO `test`.`e2e_table_source` (BIGINT_COL, LARGEINT_COL, SMALLINT_COL, TINYINT_COL, BOOLEAN_COL, DECIMAL_COL, DOUBLE_COL, FLOAT_COL, INT_COL, CHAR_COL, VARCHAR_11_COL, STRING_COL, DATETIME_COL, DATE_COL)
VALUES
(1234, 1123456, 12, 1, 0, 2222243.2222243, 2222243.22222, 1.22222, 12,
 'a', 'VARCHAR_COL', 'STRING_COL',
'2022-08-13 17:35:59', '2022-08-13')
"""