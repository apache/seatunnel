<?xml version="1.0" encoding="UTF-8"?>
<!--
    Licensed to the Apache Software Foundation (ASF) under one or more
    contributor license agreements.  See the NOTICE file distributed with
    this work for additional information regarding copyright ownership.
    The ASF licenses this file to You under the Apache License, Version 2.0
    (the "License"); you may not use this file except in compliance with
    the License.  You may obtain a copy of the License at
       http://www.apache.org/licenses/LICENSE-2.0
    Unless required by applicable law or agreed to in writing, software
    distributed under the License is distributed on an "AS IS" BASIS,
    WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
    See the License for the specific language governing permissions and
    limitations under the License.
-->
<project xmlns="http://maven.apache.org/POM/4.0.0" xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
         xsi:schemaLocation="http://maven.apache.org/POM/4.0.0 http://maven.apache.org/xsd/maven-4.0.0.xsd">
    <modelVersion>4.0.0</modelVersion>
    <parent>
        <groupId>org.apache.seatunnel</groupId>
        <artifactId>seatunnel-connector-v2-e2e</artifactId>
        <version>${revision}</version>
    </parent>

    <artifactId>connector-hudi-e2e</artifactId>
    <name>SeaTunnel : E2E : Connector V2 : Hudi</name>

    <properties>
        <testcontainer.version>1.19.1</testcontainer.version>
        <minio.version>8.5.6</minio.version>
    </properties>

    <dependencyManagement>
        <dependencies>
            <dependency>
                <groupId>org.apache.seatunnel</groupId>
                <artifactId>connector-jdbc</artifactId>
                <version>${project.version}</version>
                <type>pom</type>
                <scope>import</scope>
            </dependency>
        </dependencies>
    </dependencyManagement>

    <dependencies>
        <!-- minio containers -->
        <dependency>
            <groupId>org.testcontainers</groupId>
            <artifactId>minio</artifactId>
            <version>${testcontainer.version}</version>
            <scope>test</scope>
        </dependency>
        <dependency>
            <groupId>io.minio</groupId>
            <artifactId>minio</artifactId>
            <version>${minio.version}</version>
            <scope>test</scope>
        </dependency>
        <dependency>
            <groupId>org.apache.seatunnel</groupId>
            <artifactId>seatunnel-hadoop3-3.4.3-uber</artifactId>
            <version>${project.version}</version>
            <classifier>optional</classifier>
            <scope>test</scope>
            <exclusions>
                <exclusion>
                    <groupId>org.apache.avro</groupId>
                    <artifactId>avro</artifactId>
                </exclusion>
            </exclusions>
        </dependency>

        <dependency>
            <groupId>org.apache.seatunnel</groupId>
            <artifactId>connector-hudi</artifactId>
            <version>${project.version}</version>
            <scope>test</scope>
        </dependency>

        <dependency>
            <groupId>org.apache.seatunnel</groupId>
            <artifactId>connector-cdc-mysql</artifactId>
            <version>${project.version}</version>
            <type>test-jar</type>
            <scope>test</scope>
        </dependency>

        <dependency>
            <groupId>org.testcontainers</groupId>
            <artifactId>mysql</artifactId>
            <version>${testcontainer.version}</version>
        </dependency>

        <dependency>
            <groupId>com.mysql</groupId>
            <artifactId>mysql-connector-j</artifactId>
            <scope>test</scope>
        </dependency>

        <!--
            HudiSeatunnelS3MultiTableIT copies seatunnel-hadoop-aws.jar into the container's lib/,
            which is where a real distribution carries S3A. The jar is staged into the test
            classpath by the maven-dependency-plugin execution below; this dependency is what makes
            `-pl :connector-hudi-e2e -am` build it in the same reactor, so that staging resolves.
            The jar is only ever read from the filesystem, never linked against here, so its
            transitive hadoop-aws and AWS SDK are excluded.
        -->
        <dependency>
            <groupId>org.apache.seatunnel</groupId>
            <artifactId>seatunnel-hadoop-aws</artifactId>
            <version>${project.version}</version>
            <classifier>optional</classifier>
            <scope>test</scope>
            <exclusions>
                <exclusion>
                    <groupId>*</groupId>
                    <artifactId>*</artifactId>
                </exclusion>
            </exclusions>
        </dependency>
    </dependencies>

    <build>
        <plugins>
            <plugin>
                <groupId>org.apache.maven.plugins</groupId>
                <artifactId>maven-dependency-plugin</artifactId>
                <executions>
                    <execution>
                        <id>copy-hudi-e2e-dependencies</id>
                        <goals>
                            <goal>copy</goal>
                        </goals>
                        <phase>process-test-resources</phase>
                        <configuration>
                            <skip>false</skip>
                            <overWriteReleases>true</overWriteReleases>
                            <artifactItems>
                                <!--
                                    S3A reaches the container as the shaded seatunnel-hadoop-aws
                                    jar, which is what lib/seatunnel-hadoop-aws.jar is in a real
                                    distribution. It carries hadoop-aws together with the AWS SDK
                                    version that hadoop-aws is compiled against, so it replaces the
                                    hadoop-aws + aws-java-sdk-bundle pair used here previously:
                                    that pair was SDK v1 and hadoop-aws 3.1.4, neither of which
                                    links against the hadoop-common the 3.4.3 uber jar now ships.
                                -->
                                <artifactItem>
                                    <groupId>org.apache.seatunnel</groupId>
                                    <artifactId>seatunnel-hadoop-aws</artifactId>
                                    <version>${project.version}</version>
                                    <classifier>optional</classifier>
                                    <destFileName>seatunnel-hadoop-aws.jar</destFileName>
                                </artifactItem>
                            </artifactItems>
                            <outputDirectory>${project.build.testOutputDirectory}/e2e-dependencies</outputDirectory>
                        </configuration>
                    </execution>
                </executions>
            </plugin>
        </plugins>
    </build>
</project>
